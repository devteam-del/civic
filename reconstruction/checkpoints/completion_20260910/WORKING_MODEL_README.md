# 全段工作量體版：第一輪補建紀錄

目前檔案：/Users/ktlu/Desktop/Civic_Rebuild_20260909_201934/CompletionPass/CIVIC_FULL_WORKING_PASS_20260910_185632.blend

Blender 已切換到 `CIVIC_COMPLETE_WORKING_ASSEMBLY`。這是依現有資料覆蓋范围補齊主要構件的估算工作版，**不是全部現地細節已還原，也未完成衛星／街景校準**。

本輪依「先建出量體、有問題先標記」執行，新增 4,954 個網格，其中 8 個屋頂被有坡道開口的版本替換，組裝場景目前使用 4,946 個新增網格。

| 類別 | 本輪成果 |
|---|---|
| 第一排建物 | 581 棟缺高度建物按附近最多5棟有高度候選的中位數估算（500m內；無資料13.2m），再補4個OSM複合建物輪廓；連同既有527棟，工作場景共1,112個建物量體。重要地標高度可能誤差很大，已保留標記。 |
| 地下停車場 | 八段補側牆、柱列、帶開口樓板、折返樓梯、機房與設備，另有1,919個車位分隔線構件；這個數字不是停車格數。配置是通用估算，非官方實際配置。 |
| 地面連接坡道 | 13條0至B1估算坡道、擋土牆、13支新增相機，八段屋頂加對應開口。既有地面／分隔島開挖及入口方向仍待修。 |
| 設備 | 45個設備量體，含16個停車場機房設備、14個官方圖示設備的估算位置、15個OSM候選設備；候選不等同確定位於分隔島。 |
| 地下街 | 新增702個柱列、隔間、燈具及R1樓梯構件。沒有編造實際店名；平面模組、入口方位、殼體身分與地面開口仍待校準。 |
| 組裝 | 地面／高架沿用既有量體，停車場、地下街、設備、第一排建物集中到單一場景；Y26 A/B比較案仍保留在原比較場景。 |
| 相機 | 組裝場景含原106支每200m南北相機、原坡道相機及13支新地面入口坡道相機。 |

## 檢查與問題標記

新增活動網格4,946個：非有限頂點、零面積面及非流形邊檢查均無異常。敦延兩層側牆因世界座標浮點精度形成的退化面已改用局部網格座標修復，未改來源外框及標高。

此檢查不代表碰撞／動線／真實比例已通過。既有布林5條異常共邊、7組柱位衝突、支承未配置、敦延／延吉重疊與本轮估算配置，均保留至後續校準。

目前637筆待處理紀錄存於本資料夾 `all_deferred_issues.json`，同時嵌入Blender文字資料 `CIVIC_DEFERRED_ISSUES.json`。`COMPLETION_ISSUE_MARKERS` 集合中的 `QA_*` 物件含位置與issue自訂屬性；無單一位置的全局問題留在文字清單。

## 檢視方式

- 全段：使用 `CIVIC_COMPLETE_WORKING_ASSEMBLY`。
- 地下：暫時關閉 `COMPLETION_GROUND_HIGHWAY`、`COMPLETION_FRONTAGE`，屋頂在 `COMPLETION_REMOVABLE_ROOFS`（預設關閉以利剖視）。
- 停車場與地下街可分別切換 `COMPLETION_PARKING`／`COMPLETION_MALLS`；新增地面坡道在 `COMPLETION_SURFACE_ACCESS`。
- 本資料夾的 `全段.png`、`中林地下.png`、`地下街工作量體.png` 是工作渲染，不是實景比對圖。

## 大關卡備份與後續順序

A/B：保留原始配準及來源，新增第一排建物高度估算與4個複合輪廓。C：八段停車場與入口工作量體已存檔。D：地下街及單場景組裝已存檔。E：本輪基本幾何檢查與待修清單已存檔。這些是工作檢查點，並非原工作表現地驗收關卡通過。

下一階段依清單先校準道路／高架／入口平面位置，再處理標高、機房尺寸及地標高度；最後處理動線交會、地面開口和逐相機實景比較。尚未開始這輪衛星與街景校準。

`.blend`、輸入資料與渲染留本機；指令與檢查紀錄備份GitHub。原場景、比較方案與修改前完整快照保留。
