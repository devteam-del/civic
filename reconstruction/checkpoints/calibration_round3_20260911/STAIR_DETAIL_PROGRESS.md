# Parking stair detail checkpoint

56 existing stair flights now have paired estimated handrails. 28 landings have outer return guards. 560 flight posts and 140 landing posts meet the modeled tread/landing surfaces. All 84 new meshes passed finite-coordinate and closed-edge checks. This is geometric modeling, not surveyed dimensions or code certification; infill, fixings and accessible-route design remain unresolved.

Gongzhong core 0: a 5m along-road offset removes hits against the selected Y/Zhongshan underground-mall meshes in a 342-sample check. The original and alternative are modeled in GONGZHONG_CORE_OFFSET_COMPARISON (orange original, green offset). The alternative remains excluded from the main working assembly. No parking slab openings, surface entrance positions or connections have been relocated. Other constraints have not been certified.

The existing 106 inward-facing road cameras and 1-second east-to-west timeline cuts, frames 1-2544 at24fps, are preserved. Previous full-site checks reported 15 blocked paths. Those conflict markers remain; this detail pass did not rerun full-site headroom validation or declare the routes clear.

Local blend snapshots precede the modeling pass. GitHub includes scripts and text checks only, without blend binaries, photos or raw datasets.
